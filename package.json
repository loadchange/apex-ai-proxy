{
	"name": "apex-ai-proxy",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "node update-wrangler-config.js && wrangler deploy && git checkout wrangler.jsonc",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.6.4",
		"@cloudflare/workers-types": "^4.20250214.0",
		"typescript": "^5.5.2",
		"vitest": "~2.1.9",
		"wrangler": "^4.28.1"
	}
}
